<h1>Creating Lists with movies</h1>
<% @lists.each do |list| %>
<ul>
  <li><%= link_to list.name, list_path(list.id) %></li>
</ul>
<% end %>

<h4><%= link_to "🤩 Create movie list", new_list_path, class: "btn btn-outline-primary btn-sm"  %></h4>
